//__dirname = path t current direc
// __filename = file name
// require = function to use modules
// module = info about module
// process - info about env where the program is being ex

setInterval(() => {
    console.log(new Date().getSeconds());
}, 1000);